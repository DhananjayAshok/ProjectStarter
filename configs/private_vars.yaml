storage_dir: "PLACEHOLDER" # Path to the directory where heavy duty storage will be done, meant for large files like models and datasets
huggingface_repo_namespace: "PLACEHOLDER" # The namespace (user or org) on the hub where the repo is located. Use this to sync large files
huggingface_repo_name: "PLACEHOLDER" # The name of the repo on the hub. Use this to sync large files